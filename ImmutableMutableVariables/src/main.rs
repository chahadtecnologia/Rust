fn main() {
    // Mutable Variable
    let mut _books: i32 = 50;
    println!("I plan to read {_books} books this year");

    // Immutable Variable
    let _books: i32 = 60;
    println!("I now plan to read {_books} books this year");
}
